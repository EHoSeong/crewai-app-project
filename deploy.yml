# - name: Deploy new application version to Kubernetes
#   hosts: localhost
#   connection: local
#   become: no
#   tasks:
#     - name: Trigger a rolling restart of the deployment
#       command: kubectl rollout restart deployment crewai-app-deployment